/**
 * PulseRank ingestion server module (scaffold).
 *
 * Agent A0 freeze placeholder. Trusted-snapshot ingestion, validation, and
 * promotion into the `pulse` schema land here. All queries must be
 * schema-qualified to `pulse.*`; legacy table reads are prohibited.
 */
export const PULSERANK_INGESTION_SERVER_PLACEHOLDER = true;
