/**
 * PulseRank collection server module (scaffold).
 *
 * Agent A0 freeze placeholder. Collection-run orchestration against the NEW
 * PulseRank Bright Data collector lands here. The legacy collector ID
 * `c_msyjsllt1r9ej5tdub` must never be invoked from this module; the new
 * collector ID arrives via `PULSERANK_COLLECTOR_ID` after agent A2 creates it.
 */
export const PULSERANK_COLLECTION_SERVER_PLACEHOLDER = true;
