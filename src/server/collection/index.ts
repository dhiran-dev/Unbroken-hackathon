/**
 * PulseRank collection server module (scaffold).
 *
 * Agent A0 freeze placeholder. Collection-run orchestration against the NEW
 * PulseRank Bright Data collector lands here. The retired legacy collector ID
 * (see AGENTS.md) must never be invoked from this module; the new collector
 * ID arrives via `PULSERANK_COLLECTOR_ID` in `@/lib/env`.
 */
export const PULSERANK_COLLECTION_SERVER_PLACEHOLDER = true;
