-- Reserved authorization marker. Image evidence is published only through the
-- explicitly linked, append-only records created by migration 0007; trusted
-- observation JSON and raw landing rows are never rewritten here.
SELECT 1;
